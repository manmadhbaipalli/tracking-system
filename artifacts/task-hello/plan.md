# Plan: task-hello

## Objective
Create a `hello_world.py` file that prints "Hello World".

## Steps
1. Create `hello_world.py` at the root of the workspace.
2. The file should contain a single `print` statement that outputs `Hello World`.
3. Document decisions in `findings.md` and summarize in `summary.md`.

## Constraints
- Only `hello_world.py` may be modified/created.
